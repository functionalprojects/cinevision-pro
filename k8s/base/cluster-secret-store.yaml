apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: cinevision-secret-store
spec:
  provider:
    aws:
      service: SecretsManager
      region: us-east-1
      auth:
        jwt:
          serviceAccountRef:
            name: cinevision-secrets-sa
            namespace: cinevision
