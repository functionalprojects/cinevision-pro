apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: cinevision-secret-store
spec:
  provider:
    aws:
      auth:
        jwt:
          serviceAccountRef:
            name: cinevision-secrets-sa-green
            namespace: cinevision-prod
