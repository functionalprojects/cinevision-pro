name: PR Validation

on:
  pull_request:
    branches:
      - develop
      - staging
      - main
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review

concurrency:
  group: pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true

env:
  TERRAFORM_VERSION: "1.6.6"
  NODE_VERSION: "18"
  JAVA_VERSION: "17"
  AWS_REGION: "us-east-1"

# =========================================================
# 1. ENHANCED CHANGE DETECTION WITH SERVICE DISCOVERY
# =========================================================
jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      # Directories that changed
      terraform: ${{ steps.filter.outputs.terraform }}
      k8s: ${{ steps.filter.outputs.k8s }}
      scripts: ${{ steps.filter.outputs.scripts }}
      docs: ${{ steps.filter.outputs.docs }}
      services: ${{ steps.filter.outputs.services }}
      
      # Service discovery and matrix
      available_services: ${{ steps.discover-services.outputs.available_services }}
      changed_services_matrix: ${{ steps.services.outputs.matrix }}
      changed_services_list: ${{ steps.services.outputs.changed_services_list }}
      
      # Legacy service outputs for backward compatibility
      api-gateway: ${{ steps.filter.outputs.api-gateway }}
      eureka-server: ${{ steps.filter.outputs.eureka-server }}
      movieService: ${{ steps.filter.outputs.movieService }}
      userService: ${{ steps.filter.outputs.userService }}
      emailService: ${{ steps.filter.outputs.emailService }}
      frontend: ${{ steps.filter.outputs.frontend }}
      
      # Change summary for debugging
      change_summary: ${{ steps.summary.outputs.summary }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Fetch base branch
        run: |
          git fetch origin ${{ github.base_ref }}:${{ github.base_ref }} --depth=1 || \
          git fetch origin ${{ github.base_ref }} --depth=1 || \
          git fetch origin --depth=1

      # =====================================================
      # SERVICE DISCOVERY - Dynamically find all services
      # =====================================================
      - name: Discover available services
        id: discover-services
        run: |
          # Find all directories under services/ that contain either package.json (frontend) or pom.xml (backend)
          SERVICES=()
          
          for dir in services/*/; do
            if [ -d "$dir" ]; then
              service_name=$(basename "$dir")
              
              # Validate it's a proper service directory
              if [ -f "${dir}package.json" ] || [ -f "${dir}pom.xml" ]; then
                SERVICES+=("\"$service_name\"")
                echo "✅ Discovered service: $service_name"
              else
                echo "⚠️ Skipping $service_name - no package.json or pom.xml found"
              fi
            fi
          done
          
          # Create JSON array
          if [ ${#SERVICES[@]} -eq 0 ]; then
            AVAILABLE_SERVICES="[]"
            echo "⚠️ No services discovered"
          else
            AVAILABLE_SERVICES="[$(IFS=,; echo "${SERVICES[*]}")]"
            echo "✅ Discovered ${#SERVICES[@]} services"
          fi
          
          echo "available_services=$AVAILABLE_SERVICES" >> $GITHUB_OUTPUT
          
          # Create a space-separated list for debugging
          SERVICES_SPACE=$(echo "$AVAILABLE_SERVICES" | jq -r '.[]' 2>/dev/null | tr '\n' ' ' || echo "")
          echo "available_services_space=$SERVICES_SPACE" >> $GITHUB_OUTPUT

      - name: Detect changed paths
        uses: dorny/paths-filter@v3
        id: filter
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          base: ${{ github.base_ref }}
          ref: ${{ github.event.pull_request.head.sha }}
          filters: |
            terraform:
              - 'terraform/**'
            k8s:
              - 'k8s/**'
            scripts:
              - 'scripts/**'
            docs:
              - '**/*.md'
              - 'docs/**'
              - 'README.md'
            services:
              - 'services/**'
            api-gateway:
              - 'services/api-gateway/**'
            eureka-server:
              - 'services/eureka-server/**'
            movieService:
              - 'services/movieService/**'
            userService:
              - 'services/userService/**'
            emailService:
              - 'services/emailService/**'
            frontend:
              - 'services/frontend/**'

      # =====================================================
      # BUILD CHANGED SERVICES MATRIX (Using shell script)
      # =====================================================
      - name: Build changed services matrix
        id: services
        run: |
          # Get all filter outputs and create a JSON object
          CHANGED_SERVICES=()
          
          # Check each known service using direct output references
          if [[ "${{ steps.filter.outputs.api-gateway }}" == "true" ]]; then
            CHANGED_SERVICES+=("\"api-gateway\"")
            echo "✅ Service changed: api-gateway"
          fi
          
          if [[ "${{ steps.filter.outputs.eureka-server }}" == "true" ]]; then
            CHANGED_SERVICES+=("\"eureka-server\"")
            echo "✅ Service changed: eureka-server"
          fi
          
          if [[ "${{ steps.filter.outputs.movieService }}" == "true" ]]; then
            CHANGED_SERVICES+=("\"movieService\"")
            echo "✅ Service changed: movieService"
          fi
          
          if [[ "${{ steps.filter.outputs.userService }}" == "true" ]]; then
            CHANGED_SERVICES+=("\"userService\"")
            echo "✅ Service changed: userService"
          fi
          
          if [[ "${{ steps.filter.outputs.emailService }}" == "true" ]]; then
            CHANGED_SERVICES+=("\"emailService\"")
            echo "✅ Service changed: emailService"
          fi
          
          if [[ "${{ steps.filter.outputs.frontend }}" == "true" ]]; then
            CHANGED_SERVICES+=("\"frontend\"")
            echo "✅ Service changed: frontend"
          fi
          
          # Build matrix output
          if [ ${#CHANGED_SERVICES[@]} -eq 0 ]; then
            echo "matrix=[]" >> $GITHUB_OUTPUT
            echo "changed_services_list=" >> $GITHUB_OUTPUT
            echo "📭 No services changed"
          else
            MATRIX="[$(IFS=,; echo "${CHANGED_SERVICES[*]}")]"
            CHANGED_LIST=$(IFS=,; echo "${CHANGED_SERVICES[*]}" | tr -d '"')
            echo "matrix=$MATRIX" >> $GITHUB_OUTPUT
            echo "changed_services_list=$CHANGED_LIST" >> $GITHUB_OUTPUT
            echo "📦 Changed services: ${CHANGED_SERVICES[*]}"
          fi

      # =====================================================
      # GENERATE CHANGE SUMMARY
      # =====================================================
      - name: Generate change summary
        id: summary
        run: |
          SUMMARY=""
          CHANGED_ITEMS=()
          
          [[ "${{ steps.filter.outputs.terraform }}" == "true" ]] && CHANGED_ITEMS+=("terraform")
          [[ "${{ steps.filter.outputs.k8s }}" == "true" ]] && CHANGED_ITEMS+=("kubernetes")
          [[ "${{ steps.filter.outputs.scripts }}" == "true" ]] && CHANGED_ITEMS+=("scripts")
          [[ "${{ steps.filter.outputs.docs }}" == "true" ]] && CHANGED_ITEMS+=("docs")
          [[ "${{ steps.filter.outputs.services }}" == "true" ]] && CHANGED_ITEMS+=("services")
          
          if [ ${#CHANGED_ITEMS[@]} -gt 0 ]; then
            SUMMARY=$(IFS=,; echo "${CHANGED_ITEMS[*]}")
          fi
          
          echo "summary=$SUMMARY" >> $GITHUB_OUTPUT
          
          echo "========================================="
          echo "📊 CHANGE DETECTION SUMMARY"
          echo "========================================="
          echo "Changed categories: ${SUMMARY:-none}"
          echo "Changed services: ${{ steps.services.outputs.changed_services_list }}"
          echo "========================================="

# =========================================================
# 2. TERRAFORM VALIDATION (Only if terraform dir changed)
# =========================================================
  terraform-validation:
    needs: detect-changes
    if: needs.detect-changes.outputs.terraform == 'true'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}

      - name: Terraform Format
        run: terraform fmt -recursive -check terraform/

      - name: Terraform Init + Validate
        run: |
          # Find directories containing .tf files
          if [ -d "terraform" ]; then
            find terraform -type f -name "*.tf" -printf "%h\n" 2>/dev/null | sort -u | while read dir; do
              echo "Validating $dir"
              (
                cd "$dir" || exit 1
                terraform init -backend=false -input=false
                terraform validate
              )
            done
          else
            echo "No terraform directory found"
          fi

# =========================================================
# 3. KUBERNETES VALIDATION (Only if k8s dir changed)
# =========================================================
  kubernetes-validation:
    needs: detect-changes
    if: needs.detect-changes.outputs.k8s == 'true'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Install kubeconform
        run: |
          curl -L https://github.com/yannh/kubeconform/releases/latest/download/kubeconform-linux-amd64.tar.gz | tar xz
          sudo mv kubeconform /usr/local/bin/

      - name: Validate changed manifests
        run: |
          if [ ! -d "k8s" ]; then
            echo "No k8s directory found"
            exit 0
          fi
          
          CHANGED_FILES=$(git diff --name-only origin/${{ github.base_ref }}...HEAD -- 'k8s/**/*.yaml' 2>/dev/null || \
                          git diff --name-only ${{ github.event.pull_request.base.sha }}...${{ github.event.pull_request.head.sha }} -- 'k8s/**/*.yaml' 2>/dev/null || \
                          echo "")
          
          if [ -z "$CHANGED_FILES" ]; then
            echo "No changed YAML files found"
            exit 0
          fi
          
          echo "Changed files:"
          echo "$CHANGED_FILES"
          
          FAILED=0
          while IFS= read -r file; do
            if [ -f "$file" ]; then
              echo "Validating $file"
              if ! kubeconform -strict "$file"; then
                FAILED=1
              fi
            fi
          done <<< "$CHANGED_FILES"
          
          if [ $FAILED -eq 1 ]; then
            exit 1
          fi

# =========================================================
# 4. SERVICES VALIDATION (ONLY when a service directory changed)
# =========================================================
  services-validation:
    needs: detect-changes
    if: |
      needs.detect-changes.outputs.services == 'true' &&
      needs.detect-changes.outputs.changed_services_matrix != '[]'
    
    runs-on: ubuntu-latest
    
    strategy:
      fail-fast: false
      matrix:
        service: ${{ fromJson(needs.detect-changes.outputs.changed_services_matrix) }}

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Validate service directory
        id: service-check
        run: |
          SERVICE_PATH="services/${{ matrix.service }}"
          
          if [ ! -d "$SERVICE_PATH" ]; then
            echo "❌ Service directory not found: $SERVICE_PATH"
            exit 1
          fi
          
          # Detect service type
          if [ -f "$SERVICE_PATH/package.json" ]; then
            echo "✅ Frontend service detected"
            echo "service_type=frontend" >> $GITHUB_OUTPUT
          elif [ -f "$SERVICE_PATH/pom.xml" ]; then
            echo "✅ Java service detected"
            echo "service_type=java" >> $GITHUB_OUTPUT
          else
            echo "❌ No package.json or pom.xml found for ${{ matrix.service }}"
            exit 1
          fi

      # -----------------------------------------
      # JAVA SERVICE SETUP
      # -----------------------------------------
      - name: Setup Java
        if: steps.service-check.outputs.service_type == 'java'
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}
          cache: maven
          cache-dependency-path: services/${{ matrix.service }}/pom.xml

      # -----------------------------------------
      # NODE SERVICE SETUP
      # -----------------------------------------
      - name: Setup Node
        if: steps.service-check.outputs.service_type == 'frontend'
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: services/${{ matrix.service }}/package-lock.json

      # -----------------------------------------
      # BUILD & TEST (Only changed services)
      # -----------------------------------------
      - name: Build and test service
        run: |
          cd services/${{ matrix.service }}
          
          if [[ "${{ steps.service-check.outputs.service_type }}" == "frontend" ]]; then
            echo "📦 Installing Node dependencies..."
            npm ci
            
            echo "🏗️ Building frontend..."
            npm run build --if-present
            
            if grep -q '"test"' package.json 2>/dev/null; then
              echo "🧪 Running tests..."
              npm test --if-present
            else
              echo "ℹ️ No tests defined"
            fi
          else
            echo "📦 Building Java service with Maven..."
            mvn -B clean verify -DskipTests=false
          fi

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports-${{ matrix.service }}-${{ github.run_id }}
          path: |
            services/${{ matrix.service }}/target/surefire-reports/
            services/${{ matrix.service }}/target/failsafe-reports/
            services/${{ matrix.service }}/test-results/
            services/${{ matrix.service }}/coverage/
          retention-days: 7
          if-no-files-found: ignore

# =========================================================
# 5. SCRIPTS VALIDATION (ONLY if scripts dir changed)
# =========================================================
  scripts-validation:
    needs: detect-changes
    if: needs.detect-changes.outputs.scripts == 'true'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Install tools
        run: |
          sudo apt-get update
          sudo apt-get install -y shellcheck python3 python3-pip ansible
          pip3 install --upgrade pytest pylint

      - name: Get changed script files
        id: changed-scripts
        run: |
          # Get all changed script files of different types
          SHELL_FILES=$(git diff --name-only origin/${{ github.base_ref }}...HEAD -- 'scripts/**/*.sh' 'scripts/**/*.bash' 2>/dev/null || echo "")
          PYTHON_FILES=$(git diff --name-only origin/${{ github.base_ref }}...HEAD -- 'scripts/**/*.py' 2>/dev/null || echo "")
          ANSIBLE_FILES=$(git diff --name-only origin/${{ github.base_ref }}...HEAD -- 'scripts/ansible/**/*.yml' 'scripts/ansible/**/*.yaml' 2>/dev/null || echo "")
          
          # Save to files to avoid command line limits
          echo "$SHELL_FILES" > /tmp/shell_files.txt
          echo "$PYTHON_FILES" > /tmp/python_files.txt
          echo "$ANSIBLE_FILES" > /tmp/ansible_files.txt
          
          if [ -n "$SHELL_FILES" ]; then
            echo "shell_files=true" >> $GITHUB_OUTPUT
          fi
          if [ -n "$PYTHON_FILES" ]; then
            echo "python_files=true" >> $GITHUB_OUTPUT
          fi
          if [ -n "$ANSIBLE_FILES" ]; then
            echo "ansible_files=true" >> $GITHUB_OUTPUT
          fi

      - name: Validate Shell Scripts
        if: steps.changed-scripts.outputs.shell_files == 'true'
        run: |
          while IFS= read -r file; do
            if [ -f "$file" ]; then
              echo "🔍 ShellCheck: $file"
              shellcheck "$file"
            fi
          done < /tmp/shell_files.txt

      - name: Validate Python Scripts
        if: steps.changed-scripts.outputs.python_files == 'true'
        run: |
          while IFS= read -r file; do
            if [ -f "$file" ]; then
              echo "🔍 Python syntax check: $file"
              python3 -m py_compile "$file"
              
              # Run pylint if available (optional)
              if command -v pylint &> /dev/null; then
                pylint --exit-zero "$file"
              fi
            fi
          done < /tmp/python_files.txt

      - name: Validate Ansible Playbooks
        if: steps.changed-scripts.outputs.ansible_files == 'true'
        run: |
          while IFS= read -r file; do
            if [ -f "$file" ]; then
              echo "🔍 Ansible syntax check: $file"
              ansible-playbook --syntax-check "$file"
            fi
          done < /tmp/ansible_files.txt

# =========================================================
# 6. DOCS VALIDATION (ONLY if docs changed)
# =========================================================
  docs-validation:
    needs: detect-changes
    if: needs.detect-changes.outputs.docs == 'true'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Get changed markdown files
        id: changed-md
        run: |
          CHANGED_MD=$(git diff --name-only origin/${{ github.base_ref }}...HEAD -- '*.md' 'docs/**/*.md' 'README.md' 2>/dev/null || \
                       git diff --name-only ${{ github.event.pull_request.base.sha }}...${{ github.event.pull_request.head.sha }} -- '*.md' 'docs/**/*.md' 'README.md' 2>/dev/null || \
                       echo "")
          
          echo "$CHANGED_MD" > /tmp/changed_md.txt
          
          if [ -n "$CHANGED_MD" ]; then
            echo "has_changed=true" >> $GITHUB_OUTPUT
          else
            echo "has_changed=false" >> $GITHUB_OUTPUT
          fi

      - name: Markdown Link Check
        uses: gaurav-nelson/github-action-markdown-link-check@v1
        with:
          use-quiet-mode: 'yes'
          check-modified-files-only: 'yes'
          base-branch: ${{ github.base_ref }}
          folder-path: './'
          file-path: './'

      - name: Validate markdown formatting
        if: steps.changed-md.outputs.has_changed == 'true'
        run: |
          while IFS= read -r file; do
            if [ -f "$file" ]; then
              echo "Checking $file"
              
              # Check for trailing whitespace
              if grep -E ' \t|  $' "$file" 2>/dev/null; then
                echo "⚠️ Warning: Trailing whitespace or tabs found in $file"
              fi
              
              # Check for broken anchor links (basic check)
              if grep -E '\[.+\]\(#[^)]+\)' "$file" | grep -vE '\(#[a-zA-Z0-9\-]+\)' > /dev/null; then
                echo "⚠️ Warning: Potentially broken anchor links in $file"
              fi
            fi
          done < /tmp/changed_md.txt

# =========================================================
# 7. SECURITY SCAN (ALWAYS RUNS - CRITICAL)
# =========================================================
  secrets-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install Gitleaks
        run: |
          set -e
          VERSION=$(curl -s https://api.github.com/repos/gitleaks/gitleaks/releases/latest | grep tag_name | cut -d '"' -f 4)
          curl -L https://github.com/gitleaks/gitleaks/releases/download/${VERSION}/gitleaks_${VERSION#v}_linux_x64.tar.gz -o gitleaks.tar.gz
          tar -xzf gitleaks.tar.gz
          sudo mv gitleaks /usr/local/bin/
          rm gitleaks.tar.gz

      - name: Run Gitleaks Scan
        run: |
          gitleaks detect --source . --redact --verbose --exit-code 1

# =========================================================
# 8. QUALITY GATE (Enhanced with detailed reporting)
# =========================================================
  quality-gate:
    if: always()
    needs:
      - detect-changes
      - terraform-validation
      - kubernetes-validation
      - services-validation
      - scripts-validation
      - docs-validation
      - secrets-scan
    
    runs-on: ubuntu-latest
    outputs:
      overall_status: ${{ steps.evaluate.outputs.status }}
    
    steps:
      - name: Evaluate all validations
        id: evaluate
        run: |
          echo "========================================="
          echo "📊 PR VALIDATION SUMMARY"
          echo "========================================="
          echo ""
          echo "📝 Change Summary: ${{ needs.detect-changes.outputs.change_summary }}"
          echo "🎯 Changed Services: ${{ needs.detect-changes.outputs.changed_services_list }}"
          echo ""
          
          FAILED=0
          SKIPPED=0
          
          # Function to check job status
          check_job() {
            local job_name=$1
            local job_result=$2
            local should_run=$3
            
            if [[ "$should_run" == "true" ]]; then
              # Job should have run - check its result
              if [[ "$job_result" == "success" ]]; then
                echo "✅ $job_name: PASSED"
              elif [[ "$job_result" == "failure" ]]; then
                echo "❌ $job_name: FAILED"
                FAILED=1
              elif [[ "$job_result" == "skipped" ]]; then
                echo "⚠️ $job_name: SKIPPED (but should have run)"
                FAILED=1
              else
                echo "⚠️ $job_name: $job_result"
                FAILED=1
              fi
            else
              # Job correctly skipped
              echo "⏭️ $job_name: SKIPPED (no changes)"
              SKIPPED=$((SKIPPED + 1))
            fi
          }
          
          # Check each validation job based on whether it should have run
          check_job "Terraform" "${{ needs.terraform-validation.result }}" "${{ needs.detect-changes.outputs.terraform == 'true' }}"
          check_job "Kubernetes" "${{ needs.kubernetes-validation.result }}" "${{ needs.detect-changes.outputs.k8s == 'true' }}"
          check_job "Services" "${{ needs.services-validation.result }}" "${{ needs.detect-changes.outputs.services == 'true' }}"
          check_job "Scripts" "${{ needs.scripts-validation.result }}" "${{ needs.detect-changes.outputs.scripts == 'true' }}"
          check_job "Documentation" "${{ needs.docs-validation.result }}" "${{ needs.detect-changes.outputs.docs == 'true' }}"
          check_job "Secrets Scan" "${{ needs.secrets-scan.result }}" "true"
          
          echo ""
          echo "========================================="
          
          if [[ $FAILED -eq 0 ]]; then
            echo "🎉 ALL VALIDATIONS PASSED"
            if [[ $SKIPPED -gt 0 ]]; then
              echo "📈 $SKIPPED validation(s) skipped (no relevant changes)"
            fi
            echo "========================================="
            echo "status=passed" >> $GITHUB_OUTPUT
            exit 0
          else
            echo "❌ VALIDATION FAILED - Please fix the errors above"
            echo "========================================="
            echo "status=failed" >> $GITHUB_OUTPUT
            exit 1
          fi

      - name: Add detailed PR comment on failure
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            const prNumber = context.payload.pull_request.number;
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const changeSummary = '${{ needs.detect-changes.outputs.change_summary }}';
            const changedServices = '${{ needs.detect-changes.outputs.changed_services_list }}';
            
            let body = `❌ **PR Validation Failed**\n\n`;
            body += `## 📋 Change Impact Analysis\n`;
            body += `**Changed components:** ${changeSummary || 'none detected'}\n`;
            body += `**Changed services:** ${changedServices || 'none'}\n\n`;
            body += `## 🔍 Failed Validations\n`;
            body += `One or more validation checks have failed. Please check the workflow logs for details.\n\n`;
            body += `## 🔧 How to Fix\n`;
            body += `1. Review the workflow run logs above\n`;
            body += `2. Address the specific validation errors\n`;
            body += `3. Push fixes to this branch (workflow will re-run)\n\n`;
            body += `**Note:** Only directories with changes are validated. If you see a validation for a component you didn't change, check your PR's change detection.\n\n`;
            body += `[View full workflow run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})`;
            
            await github.rest.issues.createComment({
              owner,
              repo,
              issue_number: prNumber,
              body: body
            });

      - name: Add success comment with change summary
        if: success() && needs.detect-changes.outputs.change_summary != ''
        uses: actions/github-script@v7
        with:
          script: |
            const prNumber = context.payload.pull_request.number;
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const changeSummary = '${{ needs.detect-changes.outputs.change_summary }}';
            const changedServices = '${{ needs.detect-changes.outputs.changed_services_list }}';
            
            let body = `✅ **PR Validation Passed**\n\n`;
            body += `## ✅ Validated Changes\n`;
            body += `**Changed components:** ${changeSummary}\n`;
            body += `**Changed services:** ${changedServices || 'none'}\n\n`;
            body += `All validations completed successfully. This PR is ready for review.`;
            
            await github.rest.issues.createComment({
              owner,
              repo,
              issue_number: prNumber,
              body: body
            });
